# attendance

Daily check-in/out, geofence, face audit fields, corrections, midnight square-off.

**Learn:** [07 Leaves & attendance](../../docs/backend/07-domain-leaves-attendance.md)

Start here: `models.py` (`AttendanceRecord`), `tasks.py` (`square_off_incomplete_checkouts`).
