# leaves

Leave types, balances (reserve/consume/release), requests, accrual Celery jobs.

**Learn:** [07 Leaves & attendance](../../docs/backend/07-domain-leaves-attendance.md)

Start here: `services.py`, `graphql/mutations.py` (`create_leave_request`), `models.py`.
