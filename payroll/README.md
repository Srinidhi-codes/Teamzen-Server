# payroll

Salary components → structures → runs → payslips. `PayrollService` owns the math.

**Learn:** [06 Payroll](../../docs/backend/06-domain-payroll.md)

Start here: `services.py`, `models.py`, `graphql/`, REST in `api_views.py`.
